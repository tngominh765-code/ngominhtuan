RewriteEngine On

# Nếu file hoặc thư mục thật tồn tại, không rewrite
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Chuyển tất cả URL đẹp về index.php?url=...
RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]
